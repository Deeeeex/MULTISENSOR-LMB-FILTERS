function context = ...
        buildFormationCoordinatorAggregationV187ExecutionContext( ...
            presetName, seed, currentTime, actionName, scheduleEnabled)
% BUILDFORMATIONCOORDINATORAGGREGATIONV187EXECUTIONCONTEXT Frozen context.

protocol = getFormationCoordinatorAggregationV187Protocol();
if ~ischar(presetName) || isempty(presetName) || ...
        ~isscalar(seed) || ~isfinite(seed) || seed ~= round(seed) || ...
        ~isscalar(currentTime) || ~isfinite(currentTime) || ...
        currentTime ~= round(currentTime) || ...
        ~ischar(actionName) || isempty(actionName) || ...
        ~isscalar(scheduleEnabled) || ...
        ~(islogical(scheduleEnabled) || isnumeric(scheduleEnabled))
    error('FormationCoordinatorV187:InvalidExecutionContext', ...
        'The V187 execution-context input is invalid.');
end
context = struct();
context.contractVersion = ...
    'formation-coordinator-aggregation-v187-context-v1';
context.capability = ...
    'formation-coordinator-aggregation-v187-development';
context.action = ...
    'filter-formation-coordinator-aggregation-v187-development';
context.protocolId = protocol.id;
context.phase = 'opened-return';
context.presetName = presetName;
context.seed = seed;
context.currentTime = currentTime;
context.measurementTimeCount = currentTime + protocol.horizonSteps - 1;
context.policyName = protocol.outcomePolicyName;
context.actionName = actionName;
context.scheduleEnabled = logical(scheduleEnabled);
context.onlineReselectionEnabled = false;
context.developmentEvidenceOnly = true;
end
