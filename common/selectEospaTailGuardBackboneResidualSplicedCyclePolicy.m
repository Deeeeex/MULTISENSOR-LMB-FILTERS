function [adjacency, details] = ...
    selectEospaTailGuardBackboneResidualSplicedCyclePolicy( ...
        context, options)
% SELECTEOSPATAILGUARDBACKBONERESIDUALSPLICEDCYCLEPOLICY Exact metric guard.

if nargin < 2 || isempty(options)
    options = struct();
end
options.protectionRiskMetric = ...
    'current-eospa';
[adjacency, details] = ...
    selectTailGuardBackboneResidualSplicedCyclePolicy( ...
        context, options);
details.mode = ...
    'backbone-residual-spliced-cycle-eospa-tail-guard-adaptive-current';
details.tailGuardObjective = ...
    'protect-current-maximum-matched-static-eospa';
details.tailGuardProtectionMetricMatchesAudit = true;
end
