# Recorded raw-association arithmetic warnings

The preserved screen log contains NumPy divide-by-zero, overflow and invalid
value warnings at the inherited `joint_math.py` raw-W matrix multiplication.
This expression recomputes the old positive mark for an assertion; it does
not produce the new negative gate. All returned mark sums passed the frozen
comparison with the recorded marks. No warning was suppressed and no
frozen code, input, tolerance or outcome was changed.

The eighteen applicable inputs are byte-identical to the inputs of the
previous independent elementwise-products and `math.fsum` check, covering
407,972 local records. The finalizer verifies that receipt, all native and
likelihood-input hashes, and the complete record counts. This arithmetic
receipt is reused evidence, not a new run of that check. The other 38 source
runs did not record raw W; only their already audited association masses
are available.

The candidate calculation uses recorded association mass and local
posterior existence. A separate current verifier rebuilt every candidate
density from encoded residuals and independently checked all 83,584 robot
frames, including all eight performance decisions. Its full log is retained.
These checks bound the numerical evidence; the cause of the NumPy warning
itself remains undetermined.
