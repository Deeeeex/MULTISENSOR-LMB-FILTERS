# Equivalent full-structure local adapter after repeated native crashes

The fresh-per-sequence retry also terminated (PID 84218; child return -9,
completion line absent) during 0000 intermittent after completing 0000
reliable. Thus process isolation alone was insufficient. Preserve both
attempt logs and the first three complete files. No failed run is a result.

Without changing the marked observation or fusion formulas, construct a
second adapter by copying the full original local-update function, exposing
W and inserting the same column likelihood-ratio multiplication immediately
after matrix generation. Keep the original diagnostic structure and normal
return path on empty/nonempty and unity/nonunity updates. The previous
adapter returned differently shaped diagnostics in those cases. This change
tests a plausible runtime interaction; the native crash's root cause remains
unproven. Do not describe it as a scientific improvement.

Check exact state and association-weight parity between both adapters on
fixed scalar and multi-object updates, original unity-mark/empty-update
parity, and the unchanged scalar Bayes equation. Freeze the added adapter,
checks and runner separately; preserve the old source manifest unchanged.
Also inline the no-age selection into a trial-local copy of the frozen ECR
fusion loop, avoiding the extra post-return object/record mutation in the
old wrapper. Verify exact object and diagnostic-record parity for every
fusion arm. This is an equivalent interface refactor, not a new rule; neither
interface change establishes the root cause of the interpreter crash.
The new runner writes results_stable and labels its execution revision.
Compare all previously completed trajectories, packet accounting and fusion
records exactly, excluding runtime and execution-revision metadata only.
Complete all nine sequences, both conditions and five arms after the checks.
