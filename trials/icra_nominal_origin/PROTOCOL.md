# Earliest nominal GCE versus No-age recursion divergence

The preceding range/intermittent one-frame experiment is closed. It showed
that a small existence-only event can change GS recursion in that exposed
case, but did not explain the original nominal GCE versus No-age losses.
This study returns to the original nominal observation model on the
already designated `v2xt_0001` failure case, both link conditions.

First trace all six existing nominal trajectories (GCE, GS, No-age, two
links) across all 240 frames. They have complete local, posterior and
association records and exact original-recursion parity from the earlier
range preflight. Compare GCE/No-age and GS/No-age from the start. Use the
already fixed probability tolerance 1e-9 and mean/covariance tolerance
1e-7; distinguish bitwise equality from these numerical tolerances.

At the first meaningful fused-distribution difference, compare both local
sources and the source-label matching. Reconstruct each method's spatial
baseline and actual Gaussian from its local prior/posterior moments.
Separate inherited log odds, selective-history change, accepted scalar
increment and spatial-normalizer change. No-age uses the ordinary source
weights and no extra current increment. This is a descriptive trace, not
a causal conclusion or an algorithm screen.

Reuse the complete predeclared GT 5 timeline with fixed 2 m matching and
existence threshold 0.001. Preserve all predictions, nearby candidates,
association columns and native scores. The six timeline subsets must
exactly agree with the preceding verified trace. No native trajectory or
earlier experiment, parameter, metadata, cutoff or source file is edited.

This trace may motivate a separately registered one-time intervention on
the original GCE recursion. Its source, components, frame, conditions,
full native controls and outcomes must be fixed before any new native
outcome. A single exposed-case repair cannot be promoted as a method or
replace a complete-cohort and independent-recording evaluation.
