# Full-trajectory Gaussian component comparisons

This supplements the already fixed Gaussian experiment without changing its
methods, continuation criteria, inputs, or outputs. Compare the fixed primary
with every declared component control: no curvature guard, no conservative
history term, no positive score constraint, and the scalar-only M-AE.

Require a complete cohort summary and successful original provenance checks
before reporting cohort statistics. Recompute the ordinary trajectory scores
and retain all paired sequence differences and descriptive sequence-bootstrap
intervals. On each pair, additionally compare position errors on exactly the
same truth instances matched by both full closed-loop trajectories. This is
distinct from substituting spatial or existence outputs on fixed inputs.

Use the original 352 B Gaussian packets and native 232 B M-AE costs here.
The separate exact-zero codec study owns its actual compressed costs. These
are all previously seen real sequences, and the additional calculations do
not create independent samples or justify selecting a secondary method.
