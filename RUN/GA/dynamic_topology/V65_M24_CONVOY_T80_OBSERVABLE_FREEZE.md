# V65 M24 convoy t=80 observable freeze

The 25 cached M24 convoy states were rescored with the frozen V65 common
network denominator.  Time 80 is the only state above the unchanged 1% event
budget (`1.030%`); the next-highest state is `0.455%`.  No tracking outcome for
t=80 was read during this scan.

The complete current-only counterfactual selected formation `[1]`, covering
`87.281%` of receiver-supported rescue.  Total cross-supported useful loss is
`0.0275%` of network reference existence, the selected useful-loss/rescue
ratio is `0.0356%`, and every formation has zero cross-supported downward
decision crossings.

This freezes convoy t=80 as the first non-radial V65 paired-development case.
The selector, 1% event budget, 80% coverage target, useful-information guard,
three-step duration, reference policy, and random-seed offset remain unchanged.
