# FoV approximation diagnostic figure

- Conclusion: component-mean visibility can differ materially from the
  predicted density's visibility, including high-existence labels, in the
  opened X36 prefix. This is not a new tracking-performance result.
- Evidence chain: (a) a recorded high-existence boundary case explains the
  geometry; it is selected for the largest discrepancy, not as typical.
  (b) all six formations show the frequency of |pD difference| >= 0.05 in all
  predicted label stages and separately among r >= 0.5 stages.
- Archetype: schematic-led composite with a recorded density example and a
  population summary. No synthetic ground-truth object, empirical gain or
  method-performance line is drawn.
- Backend/export: Python/matplotlib only; 178 by 82 mm, editable SVG and
  TrueType PDF, 600-dpi PNG. Numerical geometry is exported from the saved
  trace; percentages come from the tracked formation-summary CSV.
- Statistics: one opened seed, 40 steps and 36 sensors. Label stages are
  dependent observations, not independent repeats. No p-values or confidence
  intervals. Numerical quadrature sensitivity is described in the report.
- Placement: experiment record only, not the main best table, approved
  method board or manuscript result figure.
