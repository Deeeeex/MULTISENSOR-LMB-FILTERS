function [artifactPath, screen] = ...
        runReceiverRelativeLabelRoleTeacherV149Screen( ...
            presetName, options)
% RUNRECEIVERRELATIVELABELROLETEACHERV149SCREEN Evaluate safe teacher.

if nargin < 2 || isempty(options)
    options = struct();
end
[artifactPath, screen] = runFormationIsolatedDualPayloadV136Screen( ...
    presetName, options, ...
    getReceiverRelativeLabelRoleTeacherV149Protocol());
end
