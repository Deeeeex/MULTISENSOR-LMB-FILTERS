function [artifactPath, screen] = ...
        runRoleMultiplexedSinglePayloadV143Screen(presetName, options)
% RUNROLEMULTIPLEXEDSINGLEPAYLOADV143SCREEN Fixed budget-feasible mechanism.

if nargin < 2
    options = struct();
end
[artifactPath, screen] = ...
    runFormationIsolatedDualPayloadV136Screen( ...
        presetName, options, ...
        getRoleMultiplexedSinglePayloadV143Protocol());
end
