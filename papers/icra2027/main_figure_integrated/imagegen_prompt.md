# Integrated mechanism figure — image-generation prompt

Use case: scientific-educational / infographic-diagram.
Asset: ONE integrated main mechanism figure for an ICRA robotics paper, later recreated as editable SVG.

Create a sophisticated, information-rich, unified scientific mechanism diagram for "Observation Recency in Bernoulli Fusion for Intermittent Multirobot Tracking". Use a wide landscape canvas around 1828 × 860 pixels, aspect ratio about 2.1:1, white background. The final print size will be about 181 × 85 mm.

CRITICAL COMPOSITION REQUIREMENT: this must be ONE continuous diagram, not three panels, not a triptych, not a row of three subfigures. No a/b/c letters, no vertical panel separators, no three-column headings. Build an asymmetric, flowing, interconnected mechanism map: a mobile-robot encounter scene blends directly into source-specific evidence streams, a central qualification junction, two staggered fusion paths, a fused posterior, and a long feedback path returning to the robots' next local update. The whole illustration must feel designed as one object. Small computational nodes are fine; three large sectional boxes are not.

Visual quality: exceptional technical conference illustration, crisp flat vector-style geometry, understated and precise, readable at paper size. Use clean Arial/Helvetica-style typography, well spaced short labels, elegant routed connectors, subtle pale fills, compact carefully drawn top-down wheeled robots, and meaningful mathematical glyphs. Avoid fake texture, gradients, heavy shadows, photorealistic scenery, decorative 3D objects, thick boxes, and generic corporate dashboard styling. White space should separate information without partitioning the composition.

Palette: dark ink #253746; blue #287FB5 consistently means spatial information; teal #008C7B means existence information; amber #CC8B42 means older direct observations; pale gray means untouched priors and background. Keep text black/dark ink; use color as a meaningful accent. Do not color a negative measurement red as if it were a confirmed false target.

The complete integrated visual story:

1. LOWER LEFT / LEFT THIRD: one coherent schematic ground-robot encounter. Draw two compact wheeled robots A and B large enough to recognize, plus two smaller peer robots farther away, with circular light-blue sensing fields, curved motion trails, a few target-hypothesis markers, and sparse dashed radio links. One main radio link joins A and B at their encounter. A has a faint previous pose and a short amber observation-history trail. B has a target-hypothesis ring INSIDE its sensing circle and a recent missed-detection indicator. This ring is a belief, not proof the target disappeared. Labels next to the actual robots: "A · older belief" and "B · recent miss". The scene is schematic, not a measured map.

2. LOCAL ESTIMATION integrated into the lower-left edge of that same scene: a compact two-node loop "Predict" → "Local LMB update". A small measurement-point glyph feeds the update. A short clock arrow from this local update feeds the age variable "Δ_j: time since direct sensing". A small label "Detection or miss" makes clear either kind of local sensing opportunity refreshes the local clock. Receiving a message does not reset that clock; if a short note is needed use "Local clock" next to the clock symbol, not a long disclaimer.

3. SOURCE EVIDENCE should be drawn as paths leaving the robot scene, NOT as a separate middle panel of three cards. A's amber stream carries a tiny density curve and labels "r_A, p_A" and "older". B's teal/blue stream carries another tiny density curve and labels "r_B, p_B" and "recent". A faint gray dashed stream labeled "Untouched prior" joins the qualification junction and ends at a gray cross. A and B streams pass through small check marks. The junction is a slim vertical filter/selector or a small rounded diamond labeled "History qualification". Under it, a separate short inlet labeled "Visible absence" branches ONLY toward the existence path; it must not feed a spatial density.

4. THE CENTRAL AND UPPER-RIGHT COMPUTATION is a single branching network, not a boxed third panel. After qualification, split into an UPPER teal existence path and a LOWER blue spatial path, with nodes intentionally staggered rather than aligned as columns. The upper path has "Recency weights" and "q_j ∝ w_j f(Δ_j)", a small decaying weight curve with a NONZERO FLOOR labeled "ρ", followed by the existence output "logit r* = Σ_j q_j logit r_j + log η_a". The lower path has "Spatial weights" and "a_j ∝ w_j", miniature input Gaussian curves combining into one fused curve, and "p*(x) = η_a⁻¹ ∏_j p_j(x)^{a_j}". Use generous space around equations; two-line equations are acceptable and preferable to tiny text.

5. A thin, visibly blue coupling arrow labeled "Spatial overlap η_a" points UP from the lower spatial product to the upper existence output. This connection is essential: the branches have different weights but remain coupled through spatial overlap. Show the upper direct-age curve tending to ρ above zero, not to zero.

6. FAR RIGHT: combine the two outputs in a small elegant posterior node labeled "Fused Bernoulli" with "{ℓ, r*, p*}" and a compact target-belief icon. From this node a thin curved feedback arrow sweeps along the bottom of the entire figure back to "Predict", labeled "Next local step". This long feedback loop visually unifies the whole composition.

Information density: include all six functional parts, but write no explanatory paragraph and avoid repeating headings. Let paths, glyphs, math, the two colors, and the feedback loop carry the explanation. There must be a continuous visual connection from real robot icons to their information streams, then to fusion and back to local tracking. Do not insert a disconnected generic data-card column.

Scientific constraints: LMB is the local tracking representation; recency affects existence weighting while ordinary spatial weights are retained on eligible inputs. Recent misses are informative. An untouched prior is excluded in this depicted case because informed inputs are present. Observable absence is existence-only evidence. The overlap term couples the two fusion branches. Do not invent experimental scores, seed IDs, audit language, neural modules, planners, or a claim of real hardware validation. This figure illustrates the algorithm, not empirical performance.
