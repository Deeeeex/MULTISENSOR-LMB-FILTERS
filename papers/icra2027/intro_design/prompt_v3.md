# Selected master: single-column typography refinement

Use case: precise-object-edit.
Edit target: the attached ICRA intro illustration, which is otherwise the selected master.
Make ONLY a publication-legibility typography correction. Keep EVERY car, building, tree, street line, sensing wedge, history path, shield, arrow, method box and bottom pill in exactly the same position, size, color and perspective. Preserve the exact 1385:1136 aspect ratio.

Increase the font size of the following small labels by approximately 30 percent: "Shared history", "Vehicle A", "Vehicle B", "Current observations", "Admit current ratios", "Spatial density", "Existence", and "shared normalizer". Use clean condensed Arial-style bold sans serif so they remain compact and legible. Keep their centers and reading order the same, except left-align Vehicle A to the current left margin and right-align Vehicle B to the current right margin so neither is clipped. The center label MUST use two lines, "Admit current" on the first line and "ratios" on the second, to fit cleanly between the two boxes above the big arrow. All labels must clear every arrow and object.
Keep "Posterior pool", "GCE", both large formulas and the weight bound at their current large size. Retain the small downward GCE-to-Existence arrow. Do not add a title, caption or any new element. Do not make any other changes to the graphic.
