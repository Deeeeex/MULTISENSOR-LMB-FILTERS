\section{Receiver-Induced Moment Exchange}
\label{sec:method}

\subsection{Canonical Label-Wise Projection}

At time $k$, source $j$ maintains an LMB posterior
\begin{equation}
\begin{split}
    \pi_j &= \{(r_j^\ell,p_j^\ell):\ell\in\mathcal L_j\},\\
    p_j^\ell(x)&=\sum_{m=1}^{M_j^\ell}\widetilde w_{jm}^\ell
    \mathcal N(x;m_{jm}^\ell,P_{jm}^\ell).
\end{split}
    \label{eq:lmb-gm}
\end{equation}
The implementation maps nonfinite or negative component weights to zero and
normalizes the remainder; it uses uniform weights only when all mapped weights
vanish. Let $\mathcal C(A)=(A+A^\top)/2$. The canonical label-wise projection
$\mathcal P$ retains $(\ell,r_j^\ell)$ and computes
\begin{equation}
 \mu_j^\ell=\sum_m\widetilde w_{jm}^\ell m_{jm}^\ell,
 \label{eq:projection}
\end{equation}
Let $e_{jm}^\ell=m_{jm}^\ell-\mu_j^\ell$. Then
\begin{equation}
 \Sigma_j^\ell=\mathcal C\!\left(\sum_m\widetilde w_{jm}^\ell
 \left[\mathcal C(P_{jm}^\ell)+
 e_{jm}^\ell(e_{jm}^\ell)^\top\right]\right).
 \label{eq:covariance}
\end{equation}
Thus, $\mathcal P$ replaces each mixture by the Gaussian with the same first
two canonical moments. It is idempotent:
$\mathcal P(\mathcal P(\pi))=\mathcal P(\pi)$.

\subsection{The Executed Presence-Conditioned Receiver}

Receiver $i$ aligns the union of source labels and applies $\mathcal P$ to each
delivered source. Let $\mathcal S_i^\ell$ contain self and delivered neighbors
carrying label $\ell$. The confirmatory path uses base Metropolis weights
$\gamma_{ij}$ for both spatial and Bernoulli fusion, and applies KLA over the
participating source subset for each active label:
\begin{equation}
 \omega_{ij}^\ell=\frac{\gamma_{ij}}
 {\sum_{q\in\mathcal S_i^\ell}\gamma_{iq}},
 \qquad j\in\mathcal S_i^\ell.
 \label{eq:active-weights}
\end{equation}
This source-indexed, presence-conditioned rule is not a full-source LMB-density
KLA that treats a missing label as $r^\ell=0$. The broader software accepts
distinct spatial and existence weights, but that generalized receiver is outside
the KLA claim and frozen experiment.

The deterministic Cholesky regularizer $\mathcal R$ returns $\mathcal C(A)$ if
factorization succeeds; otherwise it returns the first successful
$\mathcal C(A)+10^t\delta I$, where
$\delta=10^{-12}\max(1,\max|\operatorname{diag}(A)|)$ and $t=0,\ldots,15$.
With $S_j^\ell=\mathcal R(\Sigma_j^\ell)$, the executed canonical update is
\begin{equation}
\begin{split}
 K_i^\ell&=\mathcal R\!\left(\sum_{j\in\mathcal S_i^\ell}
 \omega_{ij}^\ell(S_j^\ell)^{-1}\right),\\
 h_i^\ell&=\sum_{j\in\mathcal S_i^\ell}\omega_{ij}^\ell
 (S_j^\ell)^{-1}\mu_j^\ell,\\
 \bar\mu_i^\ell&=(K_i^\ell)^{-1}h_i^\ell,\qquad
 \bar\Sigma_i^\ell=\mathcal R((K_i^\ell)^{-1}).
\end{split}
 \label{eq:gaussian-fusion}
\end{equation}
Define $\lambda(A)=d\log(2\pi)+\log|A|$, evaluated from the Cholesky factor.
The implementation's overlap factor is
\begin{equation}
\begin{split}
 \log\eta_{i,\mathcal R}^\ell={}&-\frac12
 \sum_{j\in\mathcal S_i^\ell}\omega_{ij}^\ell
 \left[(\mu_j^\ell)^\top(S_j^\ell)^{-1}\mu_j^\ell+
 \lambda(S_j^\ell)\right]\\
 &+\frac12\left[(\bar\mu_i^\ell)^\top K_i^\ell\bar\mu_i^\ell+
 \lambda(\bar\Sigma_i^\ell)\right].
\end{split}
 \label{eq:gaussian-overlap}
\end{equation}
Without added jitter, Eq.~\eqref{eq:gaussian-overlap} is the weighted Gaussian-
product integral; the stated form is exact for the executed regularized path.
With products below over $j\in\mathcal S_i^\ell$, the Bernoulli update is
\begin{equation}
 q_1^\ell=\eta_{i,\mathcal R}^\ell
 \prod_j(r_j^\ell)^{\omega_{ij}^\ell},\;
 q_0^\ell=\prod_j(1-r_j^\ell)^{\omega_{ij}^\ell},\;
 \bar r_i^\ell=\frac{q_1^\ell}{q_0^\ell+q_1^\ell}.
 \label{eq:existence-fusion}
\end{equation}
Each $r_j^\ell$ is clamped to $[10^{-9},1-10^{-9}]$, and the final denominator
is lower-bounded by machine epsilon. Denote
Eqs.~\eqref{eq:gaussian-fusion}--\eqref{eq:existence-fusion} by
$\mathcal G_{\omega,\mathcal R}$ and the receiver by
$\mathcal F_{\omega,\mathcal R}=\mathcal G_{\omega,\mathcal R}\circ\mathcal P$.

Let $\mathcal T$ be the posterior-content map of the versioned encode/decode
transport below. It preserves labels, $r$, weights, and means, and replaces each
component covariance by $\mathcal C(P)$. Thus, on admissible messages,
$\mathcal P\circ\mathcal T=\mathcal P$ and
$\mathcal T\circ\mathcal P=\mathcal P$.

\textbf{Proposition 1 (executed fusion-output equivalence).}
Assume identical active-label presence, base Metropolis weights, and delivery
masks; common $\mathcal C$, $\mathcal P$, $\mathcal R$, and $\mathcal T$; and
disabled covariance inflation. Then
\begin{equation}
 \mathcal F_{\omega,\mathcal R}(\mathcal T\pi_1,\ldots,\mathcal T\pi_n)=
 \mathcal F_{\omega,\mathcal R}(\mathcal T\mathcal P\pi_1,\ldots,
 \mathcal T\mathcal P\pi_n).
 \label{eq:commutation}
\end{equation}
\emph{Proof.} The identities above give
$\mathcal P\mathcal T\pi_j=\mathcal P\mathcal T\mathcal P\pi_j=
\mathcal P\pi_j$. They preserve each source-label's $r,\mu,\Sigma$ and
presence in $\mathcal S_i^\ell$. Hence the active $\omega_{ij}^\ell$,
regularized $S_j^\ell$, canonical $K_i^\ell,h_i^\ell$, overlap
$\eta_{i,\mathcal R}^\ell$, and $q_0^\ell,q_1^\ell$ are unchanged, yielding
identical $\bar r_i^\ell,\bar\mu_i^\ell,\bar\Sigma_i^\ell$. $\square$

We call $\mathcal P(\pi)$ \emph{fusion-sufficient} only for this receiver:
members of its equivalence class produce the same output.
Equation~\eqref{eq:commutation} does not imply equal posterior densities. It can
fail for a mixture-aware consumer, different label selection or numerical maps,
quantization, covariance inflation, or multi-round reuse.

\subsection{Versioned Message and Byte Accounting}

Both arms use the same versioned, little-endian binary codec underlying
$\mathcal T$: a 24-byte message
header, a 20-byte label header, and binary64 component fields for weight, mean,
and the upper covariance triangle. The encoder applies $\mathcal C$ before
packing, and the decoder reconstructs the symmetric binary64 covariance. For
state dimension $d$, a message with $M_\ell$ components per label has
\begin{equation}
 B=24+\sum_{\ell}\left[20+8M_\ell
 \left(1+d+\frac{d(d+1)}{2}\right)\right]\ \text{bytes}.
 \label{eq:wire-bytes}
\end{equation}
The full message retains every $M_\ell$ in Eq.~\eqref{eq:lmb-gm}; the proposed
message sets $M_\ell=1$ using Eqs.~\eqref{eq:projection}--\eqref{eq:covariance}.
Attempted bytes are counted after encoding and before the drop draw; delivered
bytes count messages decoded after successful delivery. No tracker variable,
scheduling decision, or fusion parameter changes.
